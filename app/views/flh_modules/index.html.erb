

<section class="flh-module">
    <div class="container grid">
        <div class="my-1">
        </div>
        <div class="card " >
            <% @flh_modules.each do |flh_module| %>
            
                <%= link_to flh_module.title, flh_module_path(flh_module) %>
                <br>
            <% end %>
        </div>
        <div class="my-1">
        </div>
    </div>
</section>